version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ../wordpress-theme/theme-config.json:/app/wordpress-theme/theme-config.json:ro
